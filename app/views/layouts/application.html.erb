<!DOCTYPE html>
<html>
  <head>
    <title>Lax</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta property "og:title" 
      content="Lax" />
    <meta property='og:image'
      content="<%= image_url('readme/splashscreenshot.png') %>"/>
    <meta property="og:description"
      content="A full-stack clone of the live-chat application Slack. Relax with friends instead of focusing on work!" />
    <meta property="og:url"
      content="http://lax-chat.herokuapp.com" />
    <%# <%= favicon_link_tag 'favicon.ico' %> %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script type='text/javascript'>
      window.splashimage = '<%= image_url('demo_splash.jpg') %>'
    </script>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
